/*using Microsoft.VisualStudio.TestTools.UnitTesting;
using Domain.Buildings.Residential;

[TestClass]
public class ResidentialBuildingTests
{
    [TestMethod]
    public void TestBuildingCreation()
    {
        var area = new Domain.Map.Area(3, 3);
        var building = new ResidentialBuilding(floors: 2, maxOccupancy: 10, area);

        Assert.AreEqual(2, building.Floors);
        Assert.AreEqual(10, building.MaxOccupancy);
        Assert.IsNotNull(building.Utilities);
    }
}
*/
