<UserControl x:Class="CitySkylines_REMAKE.Views.components.Map"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:CitySkylines_REMAKE.Views"
             xmlns:vm="clr-namespace:CitySkylines_REMAKE.ViewModels"
             xmlns:local="clr-namespace:CitySkylines_REMAKE.Views.components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:MapVM/>
    </UserControl.DataContext>

    <ScrollViewer 
        x:Name="MapScrollViewer"
        HorizontalScrollBarVisibility="Hidden"
        VerticalScrollBarVisibility="Hidden"
        PreviewMouseWheel="MapScrollViewer_PreviewMouseWheel"
        MouseDown="MapScrollViewer_MouseDown"
        MouseMove="MapScrollViewer_MouseMove"
        MouseUp="MapScrollViewer_MouseUp"
        Background="LightGray">

        <Grid x:Name="MapGrid" 
              Width="1000" 
              Height="1000" 
              RenderTransformOrigin="0.5,0.5">

            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="ScaleTransform"/>
                    <TranslateTransform x:Name="TranslateTransform"/>
                </TransformGroup>
            </Grid.RenderTransform>

            <views:MapView DataContext="{Binding MapVM}" 
                           Grid.Row="1" Grid.Column="1" 
                           BorderThickness="2" BorderBrush="Black"/>

        </Grid>
    </ScrollViewer>
</UserControl>