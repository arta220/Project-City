<Window x:Class="CitySimulatorWPF.Views.dialogs.SellMaterialsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CitySimulatorWPF.Views.dialogs"
        mc:Ignorable="d"
        Title="SellMaterialsDialog" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
               Text="Выберите материалы для продажи:" 
               FontSize="16" 
               FontWeight="Bold"
               Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" 
                            BorderThickness="1" 
                            Padding="10" 
                            Margin="0,0,0,10"
                            Background="White">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{Binding Owned, StringFormat='В наличии: {0}'}" Foreground="Gray"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="10,0" Text="{Binding Price, StringFormat='Цена: {0:N0}'}" />

                                <TextBox Grid.Column="2" Text="{Binding SelectedQuantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalContentAlignment="Center"
                                     HorizontalContentAlignment="Center"
                                     Margin="5,0"/>

                                <TextBlock Grid.Column="3"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"
                                       MinWidth="80"
                                       Text="{Binding Total, StringFormat='Итого: {0:N0}'}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="2" 
            Background="LightYellow" 
            Padding="10" 
            Margin="0,10,0,10"
            BorderBrush="Gold"
            BorderThickness="2">
            <TextBlock x:Name="TotalRevenueText" 
                   Text="Общая выручка: 0 валюты" 
                   FontSize="16" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="3" 
                Orientation="Horizontal" 
                HorizontalAlignment="Right">
            <Button Content="Продать" 
                Width="100" 
                Height="35"
                Margin="0,0,10,0"
                Background="LightGreen"
                FontWeight="Bold"
                Command="{Binding SellCommand}"/>
            <Button Content="Отмена" 
                Width="100" 
                Height="35"/>
        </StackPanel>
    </Grid>
</Window>
